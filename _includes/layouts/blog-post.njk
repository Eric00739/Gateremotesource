<!DOCTYPE html>
<html lang="{{ lang or site.lang }}" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | {{ site.blogTitle }}</title>
    <meta name="description" content="{{ summary }}">
    {% set keywordItems = { "tags": tags, "category": category, "brand": brand, "frequency": frequency, "scene": scene } | keywordList %}
    {% set keywordString = keywordItems | unique | join(', ') %}
    {% set contentTags = tags | contentTags %}
    {% set canonicalUrl = site.url ~ (page.url | canonicalPath) %}
    {% set defaultHero = hero_image or '/logo/logo.png' %}
    {% set ogImages = (og_images or defaultHero) | asArray %}
    {% set ogPrimary = ogImages[0] %}
    {% if ogPrimary.url %}
    {% set ogPrimaryPath = ogPrimary.url %}
    {% else %}
    {% set ogPrimaryPath = ogPrimary %}
    {% endif %}
    {% set heroUrl = site.url ~ ogPrimaryPath %}
    <meta name="keywords" content="{{ keywordString }}">
    <meta name="author" content="{{ author or 'GateRemoteSource' }}">
    <link rel="canonical" href="{{ canonicalUrl }}">
    
    <script>
        (function() {
            var preferredHost = "www.gateremotesource.com";
            var currentProtocol = location.protocol;
            var currentHost = location.hostname;
            
            if (currentProtocol !== "https:") {
                var target = "https://" + currentHost + location.pathname + location.search + location.hash;
                if (target !== location.href) {
                    location.replace(target);
                }
                return;
            }
            
            if (currentHost !== preferredHost) {
                var target = "https://" + preferredHost + location.pathname + location.search + location.hash;
                if (target !== location.href) {
                    location.replace(target);
                }
            }
        })();
    </script>
    
    <meta property="og:type" content="article">
    <meta property="og:site_name" content="{{ site.name }}">
    <meta property="og:locale" content="en_US">
    <meta property="og:title" content="{{ title }} | {{ site.blogTitle }}">
    <meta property="og:description" content="{{ summary }}">
    <meta property="og:url" content="{{ canonicalUrl }}">
    {% for image in ogImages %}
    {% if image.url %}
    {% set imagePath = image.url %}
    {% set imageAlt = image.alt or hero_alt or title %}
    {% else %}
    {% set imagePath = image %}
    {% set imageAlt = hero_alt or title %}
    {% endif %}
    <meta property="og:image" content="{{ site.url ~ imagePath }}">
    <meta property="og:image:alt" content="{{ imageAlt }}">
    {% endfor %}
    {% if date %}
    <meta property="article:published_time" content="{{ date | htmlDate }}">
    {% endif %}
    {% if category %}
    <meta property="article:section" content="{{ category }}">
    {% endif %}
    {% if contentTags.length %}
    {% for tag in contentTags %}
    <meta property="article:tag" content="{{ tag }}">
    {% endfor %}
    {% endif %}
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ title }} | {{ site.blogTitle }}">
    <meta name="twitter:description" content="{{ summary }}">
    <meta name="twitter:image" content="{{ heroUrl }}">
    
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "BlogPosting",
        "headline": {{ title | json }},
        "description": {{ summary | json }},
        "image": {{ heroUrl | json }},
        "datePublished": {{ (date | htmlDate) | json }},
        "dateModified": {{ (date | htmlDate) | json }},
        "author": {
            "@type": "Person",
            "name": {{ (author or "GateRemoteSource") | json }}
        },
        "publisher": {
            "@type": "Organization",
            "name": {{ site.name | json }},
            "logo": {
                "@type": "ImageObject",
                "url": {{ (site.url ~ "/logo/logo.png") | json }}
            }
        },
        "mainEntityOfPage": {
            "@type": "WebPage",
            "@id": {{ canonicalUrl | json }}
        },
        "inLanguage": {{ (lang or site.lang) | json }},
        "keywords": {{ keywordString | json }}
    }
    </script>
    
    <link rel="icon" type="image/x-icon" href="/logo/favicon.ico?v=1">
    <link rel="icon" type="image/png" sizes="32x32" href="/logo/favicon-32.png?v=1">
    <link rel="icon" type="image/png" sizes="16x16" href="/logo/favicon-16.png?v=1">
    <link rel="apple-touch-icon" sizes="180x180" href="/logo/apple-touch-icon.png?v=1">
    <link rel="manifest" href="/site.webmanifest?v=1">
    
    <link rel="preconnect" href="https://www.googletagmanager.com">
    <link rel="preload" href="/assets/css/tailwind.css" as="style">
    <link rel="stylesheet" href="/assets/css/tailwind.css">
    <noscript><link rel="stylesheet" href="/assets/css/tailwind.css"></noscript>
    
    <link rel="preload" href="/assets/vendor/fontawesome/css/all.min.css" as="style">
    <link rel="stylesheet" href="/assets/vendor/fontawesome/css/all.min.css">
    <noscript><link rel="stylesheet" href="/assets/vendor/fontawesome/css/all.min.css"></noscript>
    
    <link rel="preload" href="/assets/css/fonts.css" as="style">
    <link rel="stylesheet" href="/assets/css/fonts.css">
    <noscript><link rel="stylesheet" href="/assets/css/fonts.css"></noscript>
    
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-ZF8T77R309"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-ZF8T77R309');
    </script>
    
    <style>
        ::-webkit-scrollbar { width: 10px; }
        ::-webkit-scrollbar-track { background: #1D1D1F; }
        ::-webkit-scrollbar-thumb { background: #6E6E73; border-radius: 5px; }
        ::-webkit-scrollbar-thumb:hover { background: #0A84FF; }
        
        /* Custom styles for special components */
        .callout {
            background: linear-gradient(135deg, rgba(10, 132, 255, 0.12), rgba(29, 29, 31, 0.92));
            border: 1px solid rgba(10, 132, 255, 0.35);
            box-shadow: 0 10px 40px rgba(0,0,0,0.3);
        }
        
        .highlight-card {
            background: #1D1D1F;
            border: 1px solid rgba(209, 209, 214, 0.22);
        }
        
        .img-frame {
            overflow: hidden;
            border: 1px solid rgba(209, 209, 214, 0.22);
            border-radius: 1rem;
            box-shadow: 0 12px 30px rgba(0,0,0,0.25);
        }
        
        .img-frame img {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
        }
        
        .toc-item {
            transition: all 0.2s ease;
        }
        .toc-item:hover {
            color: #0A84FF;
            padding-left: 0.5rem;
        }
        
        .toc-item.active {
            color: #0A84FF;
            font-weight: 600;
        }
        
        .progress-bar {
            position: fixed;
            top: 0;
            left: 0;
            height: 3px;
            background: linear-gradient(90deg, #0A84FF, #0071E3);
            z-index: 9999;
            transition: width 0.1s ease;
        }
        
        .back-to-top {
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }
        .back-to-top.visible {
            opacity: 1;
            visibility: visible;
        }
    </style>
</head>
<body class="font-sans text-brand-light/80 bg-brand-dark/70 backdrop-blur selection:bg-brand-accent selection:text-white flex flex-col min-h-screen">
    <!-- Reading Progress Bar -->
    <div id="progressBar" class="progress-bar" style="width: 0%"></div>
    
    <!-- Navigation -->
    <nav class="fixed w-full z-50 bg-brand-dark/70 backdrop-blur border-b border-brand-dark/60 transition-all duration-300">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-20">
                <div class="flex-shrink-0 flex items-center gap-3">
                    <a href="/blog/" class="text-brand-accent font-bold text-sm flex items-center gap-2 hover:text-brand-light transition">
                        <i class="fa-solid fa-arrow-left text-xs"></i>
                        Back to Blog
                    </a>
                </div>
                
                <div class="flex items-center gap-3 text-xs text-brand-light/70">
                    {% if category %}
                    <span class="px-3 py-1 rounded-full bg-brand-accent/15 text-brand-accent font-bold uppercase tracking-wide">{{ category }}</span>
                    {% endif %}
                    <span class="text-brand-light/60">{{ date | htmlDate }}</span>
                    {% if read_time %}
                    <span class="text-brand-light/60">{{ read_time }}</span>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 pt-24">
        <article class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
                <!-- Article Content -->
                <div class="lg:col-span-2">
                    <!-- Article Header -->
                    <header class="mb-12 space-y-6">
                        <div class="flex flex-wrap items-center gap-3 text-xs">
                            {% if category %}
                            <span class="px-3 py-1 rounded-full bg-brand-accent/15 text-brand-accent font-bold uppercase tracking-[0.15em]">{{ category }}</span>
                            {% endif %}
                            <span class="text-brand-light/70">{{ date | readableDate }}</span>
                            {% if read_time %}
                            <span class="text-brand-light/70">{{ read_time }}</span>
                            {% endif %}
                        </div>
                        
                        <h1 class="text-4xl md:text-5xl font-bold text-white leading-tight">{{ title }}</h1>
                        
                        <p class="text-brand-light/80 text-xl leading-relaxed max-w-3xl">{{ summary }}</p>
                        
                        <div class="flex flex-wrap items-center gap-4 text-sm text-brand-light/70">
                            <span>By <span class="text-white font-semibold">{{ author or 'GateRemoteSource' }}</span></span>
                            {% if author_role %}
                            <span class="px-3 py-1 rounded-full bg-brand-surface text-brand-dark">{{ author_role }}</span>
                            {% endif %}
                        </div>
                        
                        {% if hero_image %}
                        <figure class="img-frame">
                            <img loading="lazy" decoding="async" src="{{ hero_image }}" alt="{{ hero_alt or title }}">
                            {% if hero_caption %}
                            <figcaption class="text-center text-brand-light/60 text-sm mt-3">{{ hero_caption }}</figcaption>
                            {% endif %}
                        </figure>
                        {% endif %}
                    </header>

                    <!-- Table of Contents -->
                    <div id="tableOfContents" class="mb-12 hidden">
                        <div class="bg-brand-surface border border-brand-border rounded-xl p-6">
                            <h3 class="text-brand-dark font-bold text-lg mb-4 flex items-center gap-2">
                                <i class="fa-solid fa-list text-brand-accent"></i>
                                Table of Contents
                            </h3>
                            <ul id="tocList" class="space-y-2 text-sm text-brand-muted"></ul>
                        </div>
                    </div>

                    <!-- Article Body -->
                    <article class="prose prose-lg prose-invert max-w-none">
                        {{ content | safe }}
                    </article>

                    <!-- Series Section -->
                    {% if series %}
                    {% set seriesPosts = collections.posts | seriesPosts(series) %}
                    {% if seriesPosts.length %}
                    <section class="mt-16 bg-brand-surface border border-brand-border rounded-2xl p-6">
                        <h3 class="text-brand-dark font-bold text-lg mb-4 flex items-center gap-2">
                            <i class="fa-solid fa-layer-group text-brand-accent"></i>
                            Series: {{ series }}
                        </h3>
                        <div class="space-y-3 text-sm text-brand-light/80">
                            {% for entry in seriesPosts %}
                            <a href="{{ entry.url }}" class="flex items-center justify-between p-3 rounded-xl bg-brand-dark/70 backdrop-blur border border-brand-dark/60 text-brand-light/80 hover:border-brand-accent transition {% if entry.url == page.url %}border-brand-accent{% endif %}">
                                <span>{{ entry.data.title }}</span>
                                {% if entry.url == page.url %}
                                <span class="text-xs text-brand-accent font-bold">Current</span>
                                {% endif %}
                            </a>
                            {% endfor %}
                        </div>
                    </section>
                    {% endif %}
                    {% endif %}

                    <!-- Related Articles -->
                    {% set related = collections.posts | relatedPosts(page, 3) %}
                    {% if related.length %}
                    <section class="mt-16 bg-brand-surface border border-brand-border rounded-2xl p-6">
                        <h3 class="text-brand-dark font-bold text-lg mb-4 flex items-center gap-2">
                            <i class="fa-solid fa-newspaper text-brand-accent"></i>
                            Related Articles
                        </h3>
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-sm text-brand-muted">
                            {% for entry in related %}
                            <a href="{{ entry.url }}" class="p-4 rounded-xl bg-brand-dark/70 backdrop-blur border border-brand-dark/60 hover:border-brand-accent transition block">
                                <p class="text-xs text-brand-accent font-bold uppercase tracking-wide mb-1">{{ entry.data.category or 'Guide' }}</p>
                                <p class="text-brand-light font-semibold mb-1">{{ entry.data.title }}</p>
                                <p class="text-brand-light/70 text-xs">{{ entry.data.summary }}</p>
                            </a>
                            {% endfor %}
                        </div>
                    </section>
                    {% endif %}

                    <!-- Tags & Share -->
                    <div class="mt-16 grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div class="space-y-4">
                            <a href="/blog/" class="inline-flex items-center text-brand-accent font-bold hover:text-brand-light transition">
                                <i class="fa-solid fa-arrow-left mr-2"></i> Back to Blog
                            </a>
                            {% set contentTags = tags | contentTags %}
                            {% if contentTags.length %}
                            <div class="flex flex-wrap gap-3 text-xs text-brand-light/80">
                                <span class="text-brand-light/60">Tags:</span>
                                {% for tag in contentTags %}
                                <span class="px-3 py-1 rounded-full border border-brand-dark/60 hover:border-brand-accent hover:text-brand-accent transition cursor-pointer">{{ tag }}</span>
                                {% endfor %}
                            </div>
                            {% endif %}
                        </div>
                        
                        <!-- Share Widget -->
                        <div class="bg-brand-surface border border-brand-border rounded-xl p-6">
                            <h4 class="text-brand-dark font-bold mb-4 flex items-center gap-2">
                                <i class="fa-solid fa-share-nodes text-brand-accent"></i>
                                Share this article
                            </h4>
                            <div class="flex gap-3">
                                <a href="https://www.linkedin.com/sharing/share-offsite/?url={{ site.url | safe }}{{ page.url | canonicalPath | safe }}" target="_blank" class="w-10 h-10 rounded-lg bg-brand-dark/70 backdrop-blur border border-brand-dark/60 text-brand-light flex items-center justify-center transition hover:bg-brand-accent hover:border-brand-accent hover:text-white" title="Share on LinkedIn">
                                    <i class="fa-brands fa-linkedin-in"></i>
                                </a>
                                <a href="https://twitter.com/intent/tweet?text={{ title | url_encode }}&url={{ site.url | safe }}{{ page.url | canonicalPath | safe }}" target="_blank" class="w-10 h-10 rounded-lg bg-brand-dark/70 backdrop-blur border border-brand-dark/60 text-brand-light flex items-center justify-center transition hover:bg-brand-accent hover:border-brand-accent hover:text-white" title="Share on Twitter">
                                    <i class="fa-brands fa-x-twitter"></i>
                                </a>
                                <a href="https://www.facebook.com/sharer/sharer.php?u={{ site.url | safe }}{{ page.url | canonicalPath | safe }}" target="_blank" class="w-10 h-10 rounded-lg bg-brand-dark/70 backdrop-blur border border-brand-dark/60 text-brand-light flex items-center justify-center transition hover:bg-brand-accent hover:border-brand-accent hover:text-white" title="Share on Facebook">
                                    <i class="fa-brands fa-facebook-f"></i>
                                </a>
                                <a href="mailto:?subject={{ title | url_encode }}&body=Check out this article: {{ site.url | safe }}{{ page.url | canonicalPath | safe }}" class="w-10 h-10 rounded-lg bg-brand-dark/70 backdrop-blur border border-brand-dark/60 text-brand-light flex items-center justify-center transition hover:bg-brand-accent hover:border-brand-accent hover:text-white" title="Share via Email">
                                    <i class="fa-solid fa-envelope"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Sidebar -->
                <aside class="space-y-8">
                    <!-- Author Card -->
                    <div class="bg-brand-surface border border-brand-border rounded-xl p-6 shadow-lg">
                        <h3 class="text-brand-dark font-bold text-lg mb-4">About the Author</h3>
                        <div class="flex items-start gap-4">
                            <div class="w-16 h-16 rounded-full bg-brand-accent/20 flex items-center justify-center text-brand-accent text-2xl font-bold flex-shrink-0">
                                {{ (author or 'G')[0] }}
                            </div>
                            <div>
                                <p class="text-brand-dark font-semibold text-lg">{{ author or 'GateRemoteSource' }}</p>
                                {% if author_role %}
                                <p class="text-brand-accent text-sm">{{ author_role }}</p>
                                {% endif %}
                                <p class="text-brand-muted text-sm mt-2">Expert in gate remote technology and RF engineering. Sharing practical insights for locksmiths and installers.</p>
                            </div>
                        </div>
                    </div>

                    <!-- CTA Card -->
                    <div class="callout rounded-2xl p-6 text-white space-y-4">
                        <h3 class="text-xl font-bold flex items-center gap-2">
                            <i class="fa-solid fa-box-open text-brand-accent"></i>
                            Need a Teardown Sample Kit?
                        </h3>
                        <p class="text-brand-light/90">See FR-4 boards, crystal oscillators, and AOI quality up close. Built for locksmiths and distributors.</p>
                        <div class="space-y-3">
                            <a href="/contact/" class="block text-center bg-brand-cta hover:bg-brand-ctaHover text-brand-surface px-4 py-3 rounded-lg font-bold transition shadow-lg" data-cta="blog_detail_teardown_kit">Quote now</a>
                            <a href="/catalog/" class="block text-center border border-brand-light/40 hover:border-brand-light text-brand-light font-bold px-4 py-3 rounded-lg transition" data-cta="blog_detail_view_catalog">View Catalog</a>
                        </div>
                        <div class="flex items-center gap-3 text-sm text-brand-light/70 pt-2">
                            <i class="fa-solid fa-shield-halved text-brand-accent"></i>
                            <span>Defect rate <0.1% | AOI inspected</span>
                        </div>
                    </div>

                    <!-- Quick Links -->
                    <div class="bg-brand-dark/70 backdrop-blur p-6 rounded-xl border border-brand-dark/60 shadow-lg">
                        <h3 class="text-brand-light font-bold text-lg mb-4 flex items-center gap-2">
                            <i class="fa-solid fa-link text-brand-accent"></i>
                            Quick Links
                        </h3>
                        <ul class="space-y-3 text-sm text-brand-light/80">
                            <li><a href="/catalog/" class="hover:text-brand-accent transition flex items-center gap-2"><i class="fa-solid fa-chevron-right text-xs text-brand-accent"></i> Browse Products</a></li>
                            <li><a href="/contact/" class="hover:text-brand-accent transition flex items-center gap-2"><i class="fa-solid fa-chevron-right text-xs text-brand-accent"></i> Get a Quote</a></li>
                            <li><a href="/about/" class="hover:text-brand-accent transition flex items-center gap-2"><i class="fa-solid fa-chevron-right text-xs text-brand-accent"></i> About Us</a></li>
                        </ul>
                    </div>

                    <!-- Newsletter Signup -->
                    <div class="bg-gradient-to-br from-brand-accent to-brand-hover p-6 rounded-xl text-center text-white shadow-xl relative overflow-hidden">
                        <div class="absolute top-0 right-0 w-24 h-24 bg-brand-light/10 rounded-full blur-2xl -mr-8 -mt-8"></div>
                        <h3 class="font-bold text-lg mb-2 relative z-10">Stay Updated</h3>
                        <p class="text-sm opacity-90 mb-4 relative z-10">Get weekly guides and tips delivered to your inbox.</p>
                        <form id="newsletterForm" class="space-y-3 relative z-10">
                            <input id="newsletterEmail" name="email" type="email" placeholder="Your email" required class="w-full bg-brand-surface/20 backdrop-blur border border-brand-light/30 rounded-lg px-4 py-3 text-brand-light placeholder-brand-light/70 focus:border-brand-accent focus:outline-none transition">
                            <button type="submit" class="w-full bg-brand-surface text-brand-accent px-4 py-3 rounded-lg font-bold hover:bg-brand-light transition">Subscribe</button>
                        </form>
                        <p id="newsletter-status" class="text-sm text-white/80 mt-2 hidden relative z-10" aria-live="polite"></p>
                    </div>
                </aside>
            </div>
        </article>
    </main>

    <!-- Footer -->
    <footer class="bg-brand-dark/70 backdrop-blur border-t border-brand-dark/60 py-10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-brand-light/60">
            <div class="flex items-center gap-2 text-white">
                <img src="/logo/logo.png" width="1024" height="1024" alt="GateRemoteSource logo" class="h-8 w-auto object-contain">
                <span class="font-bold">GateRemote<span class="text-brand-accent">Source</span></span>
            </div>
            <div class="flex items-center gap-4">
                <a href="/catalog/" class="hover:text-brand-accent transition">Catalog</a>
                <a href="/contact/" class="hover:text-brand-accent transition">Contact</a>
                <a href="/blog/" class="hover:text-brand-accent transition">Blog</a>
            </div>
            <span class="text-xs">&copy; 2026 GateRemoteSource. All rights reserved.</span>
        </div>
    </footer>

    <!-- Back to Top Button -->
    <button id="backToTop" class="back-to-top fixed bottom-6 right-6 z-40 bg-brand-accent hover:bg-brand-hover text-white rounded-full p-3 shadow-lg flex items-center justify-center transition" aria-label="Back to top">
        <i class="fa-solid fa-arrow-up"></i>
    </button>

    <!-- WhatsApp Button -->
    <a href="https://wa.me/8615899648898" target="_blank" class="fixed bottom-6 left-6 z-50 bg-brand-accent hover:bg-brand-hover text-white rounded-full p-4 shadow-lg flex items-center justify-center transition transform hover:scale-110 animate-bounce-slow">
        <i class="fa-brands fa-whatsapp text-4xl"></i>
    </a>

    <script type="application/ld+json">
        {{ {
            "@context": "https://schema.org",
            "@type": "BlogPosting",
            "headline": title,
            "description": summary,
            "image": heroUrl,
            "author": {
                "@type": "Person",
                "name": author or "GateRemoteSource"
            },
            "publisher": {
                "@type": "Organization",
                "name": site.name,
                "logo": {
                    "@type": "ImageObject",
                    "url": site.url + "/logo/logo.png"
                }
            },
            "datePublished": date | htmlDate,
            "mainEntityOfPage": site.url + page.url
        } | json | safe }}
    </script>

    <script>
        // Reading Progress
        window.addEventListener('scroll', () => {
            const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (winScroll / height) * 100;
            const progressBar = document.getElementById('progressBar');
            if (progressBar) {
                progressBar.style.width = scrolled + '%';
            }
            
            // Back to top button visibility
            const backToTop = document.getElementById('backToTop');
            if (backToTop) {
                if (winScroll > 500) {
                    backToTop.classList.add('visible');
                } else {
                    backToTop.classList.remove('visible');
                }
            }
        });

        // Back to top functionality
        const backToTopBtn = document.getElementById('backToTop');
        if (backToTopBtn) {
            backToTopBtn.addEventListener('click', () => {
                window.scrollTo({ top: 0, behavior: 'smooth' });
            });
        }

        // Generate Table of Contents
        document.addEventListener('DOMContentLoaded', () => {
            const articleContent = document.querySelector('.article-content');
            const tocContainer = document.getElementById('tableOfContents');
            const tocList = document.getElementById('tocList');
            
            if (!articleContent || !tocContainer || !tocList) return;
            
            const headings = articleContent.querySelectorAll('h2, h3, h4');
            
            if (headings.length < 2) {
                tocContainer.classList.add('hidden');
                return;
            }
            
            tocContainer.classList.remove('hidden');
            
            headings.forEach((heading, index) => {
                const id = 'heading-' + index;
                heading.id = id;
                
                const li = document.createElement('li');
                li.className = 'toc-item';
                
                const a = document.createElement('a');
                a.href = '#' + id;
                a.textContent = heading.textContent;
                a.className = heading.tagName === 'H2' ? 'font-semibold text-brand-light' : 'text-brand-light/80';
                
                li.appendChild(a);
                tocList.appendChild(li);
                
                // Smooth scroll to heading
                a.addEventListener('click', (e) => {
                    e.preventDefault();
                    heading.scrollIntoView({ behavior: 'smooth', block: 'start' });
                });
            });
        });

        // Active heading in TOC on scroll
        let currentActiveHeading = null;
        window.addEventListener('scroll', () => {
            const articleContent = document.querySelector('.article-content');
            if (!articleContent) return;
            
            const headings = articleContent.querySelectorAll('h2, h3, h4');
            const tocItems = document.querySelectorAll('.toc-item');
            
            let activeHeading = null;
            
            headings.forEach((heading, index) => {
                const rect = heading.getBoundingClientRect();
                if (rect.top <= 150) {
                    activeHeading = index;
                }
            });
            
            if (activeHeading !== currentActiveHeading && tocItems[activeHeading]) {
                tocItems.forEach(item => item.classList.remove('active'));
                tocItems[activeHeading].classList.add('active');
                currentActiveHeading = activeHeading;
            }
        });

        // Newsletter form
        const newsletterForm = document.getElementById('newsletterForm');
        if (newsletterForm) {
            const statusEl = document.getElementById('newsletter-status');
            const emailInput = document.getElementById('newsletterEmail');

            newsletterForm.addEventListener('submit', async (event) => {
                event.preventDefault();

                const emailValue = emailInput ? emailInput.value.trim() : '';
                if (!emailValue) {
                    if (statusEl) {
                        statusEl.classList.remove('hidden');
                        statusEl.textContent = 'Please enter a valid email.';
                    }
                    return;
                }

                if (statusEl) {
                    statusEl.classList.remove('hidden');
                    statusEl.textContent = 'Sending...';
                }

                try {
                    const formData = new FormData();
                    formData.append('email', emailValue);
                    formData.append('source', 'blog_post_newsletter');
                    const response = await fetch('https://formspree.io/f/xqakpznl', {
                        method: 'POST',
                        headers: { 'Accept': 'application/json' },
                        body: formData
                    });

                    if (response.ok) {
                        if (statusEl) statusEl.textContent = 'Thanks! You are subscribed.';
                        newsletterForm.reset();
                    } else {
                        if (statusEl) statusEl.textContent = 'Send failed. Please email sales@gateremotesource.com.';
                    }
                } catch (error) {
                    if (statusEl) statusEl.textContent = 'Network issue. Please email sales@gateremotesource.com.';
                }
            });
        }
    </script>

    <style>
        @keyframes bounce-slow { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-10px); } }
        .animate-bounce-slow { animation: bounce-slow 3s infinite; }
    </style>
</body>
</html>
